#
# Dailex project settings.
#

name: Dailex
version: ${app.version}
description: 'Daikon CQRS & ES integration with the Silex framework'
prefix: dailex
framework:
  trusted_proxies:
    - 0.0.0.0/0
mailer:
  from_email: dailex@dailex.dev
  from_name: Dailex
translator:
  default_locale: en
  locale_fallbacks:
    - en
    - de
    - fr
    - es
    - ja
    - ar